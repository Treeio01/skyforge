includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app/

    level: 6

    ignoreErrors:
        - identifier: missingType.generics

    excludePaths:
        - app/MoonShine/

